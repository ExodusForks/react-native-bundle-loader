# Treat the lockfile as the source of truth on every install.
--install.frozen-lockfile true
--install.ignore-optional true
--install.no-progress true

# Always pin exact versions when adding new dependencies.
save-prefix ""
save-exact true
